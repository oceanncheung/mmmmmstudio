# Cargo Round 13 — palette v2 + content/geometry sync + 4 new bands

Status: PLAN (Ocean reviewing). Trigger: Ocean 2026-07-10 "let's make a round
of update on cargo first" + numbered lists (palette, anyday video, sizes,
texts + ella/travis urls, text widths, icon rotation, gap tokens, desc fills,
panel picker state, image conversion, transparency).

## Recon so far (Figma truth, 2026-07-10)
- 13 showcase bands now (live has 9). NEW: band 10 = Random Pics tile band
  (caption "The algorithm of a fallen snow angel wearing Chane..." + 432x667
  slideshow webm, row 747, NO Project Description instance); band 11 = PURE
  LOVE (純愛) (5 items, row 784; item 407:565 at y783.9 h873 EXTENDS BELOW
  the row — verify intent); band 12 = Dead Good (5 slots; 1 gif + 1 mp4 per
  red notes); band 13 = WTW? (What's the World...) (lanyard, cover, gif
  images, Frame 4634 composite 1040x504, grid-animation video).
- CHANGED existing: touchbaes row 504->606.5 (sticker 262.5x606.5 etc.);
  Mandy row 618->900 (SATAIC 675x900); AnyDay (money-moments now slot 2 at
  308x308, image6 435x379, scroller 352x469); Kelly grid 1047x789 (cell
  resize); EVIIVE items resized (432x372/279x240/443x372/558x372/670x372);
  V7/Loop/Curate/Montran rivers look unchanged (verify by diff).
- Row gaps: uniformly 32 at river level; the "intricate" gaps = inner-tile
  stacks (caption rows, 4632/4634 composites) — measure + tokenize Phase 0.
- 9 new red notes (414:*): WTW x6 (grid animation-web.mp4, Image 6 gif,
  grid image square mp4, grid logo text-centered square mp4, Image 1 gif,
  wordmark-web.mp4), Dead Good x2 (DG_Image 2-5s gif, DG_slides
  animation-cargo.mp4), Random Pics x1 (Slideshow-cargo-light.webm).
  AnyDay's first-video note = likely the EDITED old note 339:755 — re-read.

## Red-note file paths (exact, from the 414:* notes — base =
## "mm.s website/Portfolio assets/_for cargo deployment/")
- WTW/for further edits/WTW_grid animation-web.mp4
- WTW/6x images for MMS_WTW_Image 6-compressed.gif
- WTW/for further edits/WTW_grid image (square)-web.mp4
- WTW/for further edits/WTW_grid logo text-centered (square)-web.mp4
- WTW/6x images for MMS_WTW_Image 1-compressed.gif
- WTW/WTW_wordmark-web.mp4
- Dead Good/6x images for MMS_DG_Image 2-5s-compressed.gif
- Dead Good/DG_slides animation-cargo.mp4
- Random Pics/Random Pics_Slideshow-cargo-light.webm
(NOTE: gif paths may sit in a "6x images for MMS" subfolder — verify with ls;
the note text wraps ambiguously. AnyDay first-video note 339:755 re-read
pending — was "XTM/Logo slideshow_AnyDay-cargo.webm", Ocean says updated.)

## Phase 0 — finish recon (Figma)
- Re-screenshot note 339:755 (anyday first video) + map notes->slots by
  arrow positions.
- Full text dump: intro, all desc instances (title/tagline/blurb/url), both
  tile captions; locate ella/travis credit lines.
- Montran caption icon rotation (Ocean: "we updated the rotation") + band-10
  icon; read exact angles.
- Inner-frame recon: WTW 4634, Kelly 4632 cells, per-tile inner gaps ->
  token table; PURE LOVE item 407:565 geometry intent.
- Transparency audit: which new exports carry alpha (sticker_full, Random
  Pics light?) -> png + pixel-verify + inline background:transparent.

## Phase 1 — palette on Cargo (mirror of Figma Final)
- tokens.css primitives: +pink #FFEEF4, green #00CD0A, brown #553D12;
  REMOVE navy/red; theme selectors renamed data-theme navy->girly,
  brown->quirky, yellow->contrast (white/black unchanged).
- panel.js: theme ids renamed + localStorage MIGRATION (mms-theme old->new
  on boot) + FIX #5: on load, set the picker's selected dot from the saved
  theme (currently the theme persists but the picker resets to white).
- Bodycopy dots (desktop panel + mobile sheet): recolor green/sky/magenta,
  order white/girly/quirky/contrast/black (Figma Swatch parity incl.
  selected state = ink outer + bg center).
- Gate: 5-theme audit on live text/plates, persistence, picker state after
  refresh, mobile sheet spot.

## Phase 2 — text pass (home page)
- Intro copy + all 13 desc copy blocks from Figma; underlined name links:
  ella -> https://ellacportfolio.com, travis -> https://www.travisleung.com.
- Montran caption new copy (custom map generator sentence; trailing ")"
  flagged to Ocean). Caption icon rotation via CSS transform if angle != 0.
- Desc widths: default col-6 stays; bands flagged "fill the showcase
  container" get inline width = container (recon Phase 0 says which).
- Text-frame width changes (captions 432-wide now on tiles etc.) applied as
  inline calc(N*var(--u)).

## Phase 3 — geometry sync on existing bands
- Per-band diff live DOM vs Figma table (this doc): resize imgs/videos,
  reorder anyday, river inline heights (touchbaes 606, mandy 900), kelly
  cell sizes, eviive sizes; keep bottom-alignment invariants.
- Tokenize the inner-tile gap set as CSS custom props (--tile-gap-* or
  reuse --space-N where values match).

## Phase 4 — media + new bands
- 9 relay uploads (mp4/gif/webm from red notes) + AnyDay first-video swap.
- Size policy (#6): videos as delivered ("-cargo/-web/-compressed" names =
  Ocean pre-compressed; only re-encode if a file > ~10MB); IMAGE exports:
  jpg q85 at 2x display size, capped ~1800px wide (freight /w/ handles
  downserve); gifs untouched (recompression kills loops).
- Figma exports for new stills (~15 @2x); png ONLY where alpha is real
  (pixel-verified), always with inline background:transparent (#7).
- Build bands 10-13 after Curate (offset/order per Figma); band 10 = tile
  pattern (caption + media, montran-style); descs per text dump.
- Booklet/map untouched.

## Phase 5 — acceptance + docs
- Full NEW-ELEMENT checklist per changed/new element (themes x5 incl. NEW
  girly/quirky/contrast values, faces, scales, shapes, geometry, served
  renditions, persistence x2, strays 0).
- DEPLOY.md Round 13, PLAYBOOK ledger, project CLAUDE.md, local tokens/site
  css mirrors. NO PUBLISH without Ocean's word.

## Open questions (Ocean)
1. Montran caption's new copy ends with ")" with no opening paren — keep or
   drop?
2. Band 10 (Random Pics) has no Project Description on Figma — intentional
   (tile-only band)?
3. PURE LOVE item 407:565 (680x873 at y784) hangs below the 784 row — a
   vertical-bleed design or accident?
4. After this round: draft-only review, or publish immediately when done?

## PHASE 0 RESULTS (execution manifest — 2026-07-10)
- OCEAN ANSWERS: montran caption fixed on figma (parens balanced); band 10
  intentionally desc-less; PURE LOVE fine (407:565 is ROTATED 90 -> visual
  873x680, fits row; my earlier read ignored rotation); DRAFT ONLY.
- ICON ROTATION = Kelly caption strip icon (442:521) rot 90 CCW -> points UP.
  CSS: transform: rotate(-90deg) on that text-icon. Montran + band-10 icons
  unchanged (right-pointing).
- ANYDAY FIRST VIDEO: same file XTM/Logo slideshow_AnyDay-cargo.webm but
  "trim out two sides for 0.5 pixel... black strip on both sides" -> ffmpeg
  crop 1px/side re-encode, re-upload, swap (slot 565x376).
- MEDIA MANIFEST (relay): WTW grid animation-web.mp4 -> 740x416 video; WTW
  Image 6 gif -> img 478x416; WTW grid image (square)-web.mp4 -> 504x504
  (Frame 4634 slot 2); WTW grid logo text-centered (square)-web.mp4 ->
  504x504 (4634 slot 1); WTW Image 1 gif -> img 593x334; WTW wordmark-web
  .mp4 -> 765x504 (takes the Cover slot per Arrow 22); DG Image 2 gif ->
  img 411x358; DG slides animation-cargo.mp4 -> 579x504 (Image 5 slot per
  Arrow 24); Random Pics Slideshow-cargo-light.webm -> 432x667 (band 10).
- EXPORTS (@2x jpg unless alpha): EVIIVE x6 NEW sizes (432x372, 279x240,
  896x504, 443x372, project-card 558x372, 670x372 — band now 6 items not 7);
  touchbaes sticker 262.5x606.5 (PNG + alpha verify); PURE LOVE x5 (627x784,
  544x680, 440x550, 873x680 [rotated node — export renders visual], 336x448);
  DG x3 (Cover 544x358, Image3 411x358, Image4 894x504); WTW Lanyard 747x504.
- GEOMETRY: touchbaes row 606.5 (only sticker resized); mandy row 900,
  SATAIC video 675x900 (css resize); anyday money 308x308 + MOVE to slot 2,
  image6 435x379, scroller 352x469; kelly grid cells 523.4x380.2 GAP 0 +
  caption strip (padT 8, icon-up + 1-line text) total 1047x789; montran/v7/
  loop/curate rivers unchanged (verify live).
- NEW BANDS after curate: 10 Random Pics (offset-2 spacer 200, tile =
  caption 432 [icon + text 409] + webm 432x667, row 747); 11 PURE LOVE
  (offset-4 spacer 432, row 784, 5 items); 12 Dead Good (no offset, row 504,
  5 slots: gif, 894 img, video 579, img 411, cover 544); 13 WTW (no offset,
  row 504: lanyard 747, wordmark video 765, gif 593x334, [2x 504 videos gap
  32], gif 478x416, video 740x416).
- TEXT: new intro (col-6 664 wide): "MM.S is a design studio specializing in
  brand identity, digital, motion, and print design. Our work and non-work
  span between corporate OOH campaigns to DIY zines." + "Everyone is a
  designer somewhere. / This is the somewhere. / Have fun with the control
  panel!" (bold headline REMOVED). All 13 desc blurbs = real copy (dumped in
  session; V7+Loop+DG desc width 1128 FILL, AnyDay 896 = 1128-offset, rest
  664). LINKS: touchbaes blurb "Ella Cheng" -> https://ellacportfolio.com;
  PURE LOVE blurb "Travis Leung" -> https://www.travisleung.com. Montran
  caption new copy (balanced parens). Kelly caption: "Known for her Hermès
  bag collection, this custom-made "Kelly's Kelly" bag is to celebrate the
  music, style, and cultural influence she brought into our world."
  URLS: Figma url rows still eviive placeholders -> keep live treatment
  (EVIIVE=eviive.ch only), flag to Ocean.
- GAPS tokenized: tile caption-to-media 8 (--space-8), river 32 (--space-32),
  kelly cells 0, WTW 4634 inner 32, kelly strip padT 8. No new tokens needed.

## EXECUTION STATE (2026-07-10 ~19:10 — paused on screen lock)
- DONE + verified: Phase 1 palette (live in draft: girly/quirky/contrast
  themes, new dots, localStorage migration, PICKER-STATE FIX confirmed);
  Phase 3 CSS rules (band captions, kelly icon rotation, kelly padding).
- LOST TO THE LOCK (replay from PHASE 0 RESULTS above): Phase 2 bodycopy
  texts + Phase 3 bodycopy geometry — Cargo's CRDT sync does not save while
  the machine screen is locked; edits reverted on reload.
- PENDING: Phase 4 (all 26 uploads + anyday swap + 4 new bands + eviive
  rebuild + touchbaes sticker swap), Phase 5 checklist/docs.
- RESUME NEEDS: the Mac awake + unlocked (lid open / display on). Relay
  server still on :8934 with 11 staged files (scratchpad/media13); figma
  export URLs expired — re-request the 15 download_assets (node ids in
  PHASE 0 RESULTS). Then: replay Phase 2 + Phase 3 bodycopy (reload-verify
  EACH batch before proceeding — see Round 13 findings), uploads, bands,
  full acceptance checklist. DRAFT ONLY (Ocean reviews).
